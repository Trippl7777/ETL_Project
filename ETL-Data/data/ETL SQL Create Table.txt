-- Create a new table
CREATE TABLE combine (

index INTEGER, 
City VARCHAR(30), 
Code VARCHAR(10), 
StartDate DATE, 
StartTime INT, 
Latitude DECIMAL, 
Longitude DECIMAL, 
Mapping VARCHAR(50), 
Weather VARCHAR(50)
);
